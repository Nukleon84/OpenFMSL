<UserControl x:Class="ChartEditor.Views.ChartEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ChartEditor.Views"
             mc:Ignorable="d" 
             xmlns:oxy="http://oxyplot.org/wpf"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <TabControl TabStripPlacement="Bottom">
            <TabItem Header="Graph">
                <Viewbox Stretch="Uniform" VerticalAlignment="Stretch" HorizontalAlignment="Center">
        <oxy:PlotView Model="{Binding Plot}" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" MinHeight="800" MinWidth="800" ></oxy:PlotView>
                </Viewbox>
            </TabItem>
            <TabItem Header="Table">
                
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
